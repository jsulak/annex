module.exports = {
  apps: [
    {
      name: 'annex',
      script: 'dist/server/index.js',
      cwd: '{{ app_dir }}',
      node_args: '--enable-source-maps',
      env: {
        NODE_ENV: 'production',
        PORT: '{{ app_port }}',
        NOTES_DIR: '{{ notes_dir }}',
        SESSION_SECRET: '{{ session_secret }}',
{% if syncthing_api_key.stdout is defined and syncthing_api_key.stdout %}
        SYNCTHING_API_KEY: '{{ syncthing_api_key.stdout }}',
{% endif %}
      },
      restart_delay: 3000,
      max_restarts: 10,
      max_memory_restart: '256M',
      kill_timeout: 5000,
    },
  ],
};
